#![allow(dead_code)] // removed when all modules are wired into CLI (Task 10)

mod backup;
mod cards;
mod collection;
mod decks;
mod notes;
mod sync;
mod tags;

fn main() {
    println!("anki-ai");
}
